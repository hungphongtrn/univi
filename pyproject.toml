[project]
name = "univi"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12.0"
dependencies = [
    "datasets>=2.14.0",
    "librosa>=0.10.0",
    "matplotlib>=3.7.0",
    "numpy>=1.24.0",
    "pillow>=10.0.0",
    "soundfile>=0.12.0",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
